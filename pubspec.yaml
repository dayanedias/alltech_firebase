name: alltech_new_firebase
description: A new Flutter application.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  #Firebase
  firebase_core: ^0.7.0
  cloud_firestore: ^0.16.0+1
  firebase_auth: ^0.20.1
  firebase_storage: ^7.0.0

  #Provider
  provider: ^5.0.0

  #utils
  flutter_neumorphic: ^3.1.0
  polygon_clipper: ^1.0.2
  syncfusion_flutter_datepicker: ^19.1.63-beta
  dropdownfield: 1.0.0
  multi_select_flutter: ^4.0.0
  carousel_slider: ^4.0.0

  image_picker: ^0.6.7+22
  file_picker: ^2.1.7

  #authentication
  rxdart: ^0.27.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  assets:
    - assets/images/Boas_vindas.png
    - assets/images/tela_de_login.png
    - assets/images/btn_login.png
    - assets/images/tela_login.png
    - assets/images/tela_inicial.png
    - assets/images/images.png
    - assets/plantacao.jpg

